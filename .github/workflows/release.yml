name: Release

on:
  push:
    tags:
      - "v*.*.*"

# Creating the release and uploading its assets writes to the repository. The default token
# grant is read-only, and a reusable workflow cannot request more than its caller was given.
permissions:
  contents: write

jobs:
  release:
    uses: damian1000/.github/.github/workflows/release.yml@main
    with:
      build-command: clean build
      # Notes only: this repository is deployed from main, and trading-system consumes its pricing
      # API through JitPack, which builds the artifact from the tag itself.
